import * as React from "react";
import Page from "../Page";
import {LangDE} from "../config/langDE";

function socialMediaPage() {
  return (
    <Page title={LangDE.socialMediaTitle}>
    </Page>
  );
}

export default socialMediaPage;
