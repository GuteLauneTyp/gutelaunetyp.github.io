import * as React from "react";
import Page from "../Page";
import {LangDE} from "../config/langDE";

function projectPage() {
  return (
    <Page title={LangDE.projectsTitle}>
    </Page>
  );
}

export default projectPage;
