import * as React from "react";
import Page from "../Page";
import {LangDE} from "../config/langDE";

function startPage() {
  return (
    <Page title={LangDE.startTitle}>
    </Page>
  );
}

export default startPage;
